[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "customer-support-agent"
version = "1.0.0"
description = "Production-like customer support agent for AgentCore Runtime"
requires-python = ">=3.10"
dependencies = [
    "aws-opentelemetry-distro~=0.18.0",
    "bedrock-agentcore>=1.9.1",
    "botocore[crt]>=1.35.0",
    "mcp~=1.24.0",
    "mcp-proxy-for-aws",
    "python-dotenv>=1.0.0",
    "strands-agents[openai]>=1.15.0",
]

[tool.hatch.build.targets.wheel]
packages = ["model"]
